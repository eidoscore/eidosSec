FROM python:3.11-slim

WORKDIR /app

# Install FastAPI and uvicorn
RUN pip install --no-cache-dir \
    fastapi==0.109.0 \
    uvicorn[standard]==0.27.0

# Copy monitoring API
COPY status-api.py .

# Create data directory
RUN mkdir -p /app/data

EXPOSE 9000

CMD ["python", "status-api.py"]
